<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">
<module name="Checker">

    <!-- Severidad por defecto -->
    <property name="severity" value="warning"/>

    <!-- ===== Checks de nivel Checker (no TreeWalker) ===== -->
    <module name="LineLength">
        <property name="max" value="120"/>
        <!-- No contar package/import ni URLs -->
        <property name="ignorePattern" value="^package|^import|a href|href|http://|https://"/>
        <!-- Opcional: si usas tabs y quieres normalizar conteo:
             <property name="tabWidth" value="2"/> -->
    </module>

    <!-- ===== Checks bajo TreeWalker ===== -->
    <module name="TreeWalker">

        <!-- IMPORTS (van en TreeWalker) -->
        <module name="AvoidStarImport"/>
        <module name="RedundantImport"/>
        <module name="UnusedImports"/>

        <!-- Estilo/espaciado/estructura -->
        <module name="OuterTypeFilename"/>
        <module name="NeedBraces"/>
        <module name="WhitespaceAfter"/>
        <module name="WhitespaceAround"/>
        <module name="OneStatementPerLine"/>
        <module name="MissingSwitchDefault"/>
        <module name="DefaultComesLast"/>
        <module name="Indentation"/>

    </module>
</module>
